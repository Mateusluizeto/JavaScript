/* Expressões e Operadores
    - Expressions
    - Operators
        Binary
        Unary
        Ternary
*/

let number = 1

console.log(++number) // Operador unários (tem 1 item)
console.log(number + 1) // Operador binario (tem 2 itens)
console.log(false ? 'oi' : 'nada') // Operador ternário (tem 3 itens) Esse é o if else simplificado

